[package]
name = "ink-counter"
version = "0.1.0"
edition = "2021"

[dependencies]
ink = { version = "5", default-features = false }

[dev-dependencies]
ink_e2e = { version = "5" }

[lib]
path = "lib.rs"

[features]
default = ["std"]
std = ["ink/std"]
ink-as-dependency = []
e2e-tests = []
